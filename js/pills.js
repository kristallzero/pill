
const pills = {};

const pillList = document.querySelector('js-pill-list')

for(let i = 0; i < pillList.children; i++) {
  const pill = pillList[i];
  
}

function createPill(pill) {
  console.log(pill);
}